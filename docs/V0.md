# V0 — current product snapshot

Status: Accepted
Scope: Non-exhaustive product-level snapshot and index of what Synthesis
currently contains.

This document answers "what does Synthesis currently contain?".

It is deliberately **not exhaustive** and it is **not the normative owner of
detailed current semantics**. Detailed accepted current truth belongs to the
domain owners under [`docs/current/`](current/). Where a detailed statement
here differs from the domain current-truth owner, **the domain owner wins for
that detail** and this snapshot should be corrected.

Use:

- [`docs/README.md`](README.md) to resolve the smallest sufficient Read Set
- [`docs/current/...`](current/) for detailed current implemented truth
- [`docs/ARCHITECTURE.md`](ARCHITECTURE.md) for durable architectural constraints
- [`docs/FUTURE.md`](FUTURE.md) for future direction (not implementation authority)
- [`docs/design/...`](design/) for feature-specific design contracts
- [`docs/work-orders/...`](work-orders/) for explicitly selected implementation deltas


## Current-truth owners

| Domain | Normative current owner |
| — | — |
| NODE-OS Shell, Home, presentation language, Terminal interface, Notes, mobile/editing presentation | [`current/INTERFACE_SHELL.md`](current/INTERFACE_SHELL.md) |
| GameState areas, local Device, represented World, servers/services, transfer capacity, System application | [`current/DEVICE_SYSTEM.md`](current/DEVICE_SYSTEM.md) |
| Scan, NodeScan, Inspect, Discovery, Service Analysis, Knowledge, Credential Access, DeviceAccess, Remote Session / RACK-OS, Authentication History | [`current/NETWORK_ACCESS.md`](current/NETWORK_ACCESS.md) |
| Filesystem, Files, FileTransfer (Download/Upload), packages, installation, removal, executables, software management | [`current/FILES_SOFTWARE.md`](current/FILES_SOFTWARE.md) |
| GameProcess runtime, scheduling, cancellation, Processes / Activity Monitor | [`current/PROCESSES_ACTIVITY.md`](current/PROCESSES_ACTIVITY.md) |
| NODE units, mining production and payout, economic recipients, `node-miner` CLI, Wallet | [`current/NODE_ECONOMY.md`](current/NODE_ECONOMY.md) |


## Product foundation

Synthesis currently runs as a responsive browser-based NODE-OS prototype.

The application uses a freshly created, versioned, sliced in-memory `GameState`
for each client session.

No save or migration system is implemented.

NODE-OS Home is a compact two-column technical control surface exposing these
player-facing applications in order:

- Terminal
- NodeScan
- Processes
- Files
- Wallet
- Notes
- System

These applications describe the current interface and are not a permanent
product taxonomy. Shell navigation is presentation state rather than gameplay
state, and the applications share one NODE-OS presentation language rather than
each owning a private one.

Mobile Safari/iPhone is a current first-class presentation target, using the
established Shell-owned Editing presentation.

Details: [`current/INTERFACE_SHELL.md`](current/INTERFACE_SHELL.md).


## Represented world

Current canonical game state includes the player's local Device, two
independent canonical economic slices (Dollars and NODE), the represented
World, and the Process, Knowledge, Discovery, DeviceAccess, RemoteSession and
FileTransfer slices.

The World currently contains the `home-net` LocalNetwork, the player's local
Device (`node-01`), two fully interactive LAN servers (`srv-01`, `srv-02`) with
represented SSH — and, on the first, HTTP — services, and additional shallow
training hosts. Both servers own RACK-OS Firmware identity, their own canonical
filesystem, concrete hardware and runtime, and a canonical network transfer
capacity. `srv-02` additionally represents a second-factor authentication
condition that a known weakness alone cannot satisfy.

Details: [`current/DEVICE_SYSTEM.md`](current/DEVICE_SYSTEM.md).


## Reconnaissance, knowledge, and access

The current investigation loop is:

```text
scan SELF
↓
discover home-net
↓
scan home-net
↓
discover a LAN server
↓
scan that server
↓
discover represented services
↓
analyze a service → vulnerability Knowledge
↓
attack that service → DeviceAccess
↓
connect → Remote Session → RACK-OS
```

Scan, Inspect and Analyze remain distinct observation roles rather than stages
of a mandatory pipeline. Remembered positive observations live in Discovery;
deeper learned information lives in Knowledge; established access is a
persistent `DeviceAccess` relationship, and an active Remote Session is a
separate operating context over it. NodeScan presents all of this from player
information only. Represented server Devices keep their own bounded
Authentication History.

Details: [`current/NETWORK_ACCESS.md`](current/NETWORK_ACCESS.md).


## Files, transfer, and software

Each represented Device owns its own canonical filesystem with three explicit
file kinds: text, software package, and executable. Remote-to-local Download
and local-to-remote Upload run as one canonical `FileTransfer` — real elapsed
network runtime that is not a `GameProcess`. A supported package that exists on
the local filesystem can be installed through Files or Terminal, which admits a
finite Software Installation Process; Software Removal is the corresponding
finite operation, started from System's inline installed-software management.
NODE Miner is the one represented runnable program: installation creates its
executable, and RUN admits it as a continuous Process — on the local Device, or
on the Device currently operated through RACK-OS.

Package, InstalledSoftware, Executable, and Process remain four distinct
things.

Details: [`current/FILES_SOFTWARE.md`](current/FILES_SOFTWARE.md).


## Processes and activity

Processes represent elapsed work and resource consumption on a concrete
executor Device. Current consumers are Service Analysis, Credential Access,
Software Installation, Software Removal, and the continuous NODE Miner;
Software Installation and NODE Miner execution may run on a represented remote
Device, using that Device's own CPU and RAM. The
Processes application is presented as the NODE-OS Activity Monitor, which
derives one view over canonical Process state and the single canonical active
FileTransfer without merging those runtime domains, and retains bounded Recent
Activity history.

Details: [`current/PROCESSES_ACTIVITY.md`](current/PROCESSES_ACTIVITY.md).


## NODE economy and Wallet

NODE is represented as canonical integer atomic units. A running NODE Miner
produces NODE from its actual allocated compute and routes completed batches to
represented economic recipients by exact address match; the unofficial NODE
Miner 1.0 release additionally diverts a share to its own embedded address. A
running Miner's payout address can be retargeted in place from the RACK-OS
Terminal without stopping it, and its Device-owned payout log records one line
per payout routing segment so earlier payouts stay attributed to the address
that actually received them.
Wallet presents the two independent canonical balances (Dollars and NODE), the
NODE address, and the NODE this Wallet actually received.

Details: [`current/NODE_ECONOMY.md`](current/NODE_ECONOMY.md).


## Not currently implemented

The following are not current implementation truth:

- filesystem writes
- filesystem permissions
- general remote execution, arbitrary program launch, or a remote shell
- remote Process observation surfaces
- per-application access permissions
- privilege escalation
- generalized exploit systems
- broader hacking mechanics
- reachability simulation
- firewall simulation
- malware simulation
- autonomous actors
- systemic authorities
- simulation-time world change
- economy simulation
- organization simulation
- multiplayer
- authoritative backend
- real networking
- saves
- migrations

Three entries previously listed here — remote filesystem access, a foreign
Remote Session operating surface, and foreign operable Firmware — were removed
during the V1 knowledge migration because current accepted `main` implements
them as RACK-OS (see [`current/NETWORK_ACCESS.md`](current/NETWORK_ACCESS.md)
and [`current/FILES_SOFTWARE.md`](current/FILES_SOFTWARE.md)). See
[`MIGRATION_NOTES_KNOWLEDGE_V1.md`](MIGRATION_NOTES_KNOWLEDGE_V1.md).

Three further entries — remote execution, remote Processes, and remote compute —
were narrowed for the same reason: remote Software Installation and remote NODE
Miner execution both run real Processes on a represented remote Device's own
CPU and RAM. What remains unimplemented is the general case listed above.

Future direction for unimplemented systems belongs to
[`docs/FUTURE.md`](FUTURE.md).

Planned work does not become current truth until it has been implemented,
reviewed, accepted, and integrated.


## Current architecture reference

This document describes what exists.

It intentionally does not redefine why the implementation is structured that
way.

Durable rules for:

- stable identity
- entity-owned truth
- World / Discovery / Knowledge separation
- shared gameplay operations
- Device / Firmware / Software / Session separation
- DeviceAccess semantics
- systemic causality
- anti-premature-abstraction discipline

belong to [`ARCHITECTURE.md`](ARCHITECTURE.md) and the modules it indexes.
