# Loaded by Vite only for `--mode online` (see `npm run dev:online` and
# `npm run build:online`). This is the one explicit switch between the
# Sandbox and Online runtimes — see src/App.tsx `onlineRuntimeEnabled`.
VITE_SYNTHESIS_ONLINE=1
