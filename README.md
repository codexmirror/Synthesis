# Synthesis V1

A local, single-player hacking game: find → scan → hack → connect → transfer → install → stronger targets.

Run `npm install`, `npm run dev`. Validate with `npm test` and `npm run build`.

V0 is frozen at `eab81314e621835bb1b14338bd772a88450f0e41`, tagged `v0-final`. All 18 former tracked root entries were V0 material and moved under `v0/` (its README is replaced with a reference notice). No runtime, tests or imports depend on it. New root tooling and CI serve V1 only.
